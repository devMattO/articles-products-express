<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    title Products
  body
    h1 Products
    ul
      each product in products
        li Name: #{product.name}
        li Price: $#{product.price}
        li Inventory: #{product.inventory}
        li Id: #{product.id}
        li

